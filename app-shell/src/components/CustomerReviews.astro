---
import { rewriteMfUrls } from "../utils/rewriteMfUrls";
const html = await fetch("http://localhost:7200/customer-reviews").then(r => r.text());
const microfrontend = rewriteMfUrls(html, "http://localhost:7200");
---
<Fragment set:html={microfrontend} />
